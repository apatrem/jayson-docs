# Intentionally empty for v1.
#
# This CLI does not call any LLM directly. The LLM is the user's own agentic
# LLM (BYO LLM, D15) — Cowork, Claude Code, Cursor, etc. — driving the skills
# under /skills/; the CLI does pure template-fill.
#
# If you ever add a standalone (in-codebase) LLM path, this is where the
# ANTHROPIC_API_KEY would go. Until then, leave this file alone.
