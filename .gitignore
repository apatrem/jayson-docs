node_modules/
dist/
.env
.env.local
.env.*.local
*.log
.DS_Store
.idea/
.vscode/settings.json
.claude/settings.local.json
coverage/
*.tsbuildinfo
tmp/
out/

# Repo reviews — kept local for review before committing. Remove these two
# lines (and `git add` the files) when you decide to track them.
docs/REPO_REVIEW.md
docs/REPO_REVIEW_2026-06-05.md

# Composio orchestrator local config (untracked during evaluation; track it
# later if you adopt Composio repo-wide).
agent-orchestrator.yaml
