# Document System — Context

The vocabulary the team uses when talking about this codebase. Definitions are kept tight on purpose — when a term needs to be clarified, this is where the resolution lives.

## Language

### Documents

**DocModel**:
The canonical JSON document conforming to the Zod schema. Every other representation — TipTap editor state, YAML on disk, rendered HTML — is a *projection* of it.
_Avoid_: "the document state", "the editor state", "the model"

**Projection**:
A derived representation of the DocModel (editor JSON, YAML, HTML, PDF). Never canonical — always reconstructible from the DocModel.
_Avoid_: "view", "format"

**Block**:
A typed, schema-validated unit inside a section or slide. Carries a stable `id` so comments and patches can anchor to it across serializations.
_Avoid_: "element", "component", "widget"

### Block tiers

The block library is closed *at any given moment* but is composed of three tiers, each with a different origin, lifecycle, and trust model.

**Standard block**:
One of the 15 generic blocks the developer ships with the app (prose, heading, callout, chart, table, …). Brand-token mapping lets each instance look native to a given consultancy without code changes.
_Avoid_: "pre-built block", "generic block", "built-in block", "Tier 1"

**Brand block**:
A block generated by the setup AI from the consultancy's demo materials, when a recurring pattern can't be expressed by a Standard block alone. Reviewed and refined once at install, then closed for that install.
_Avoid_: "client block", "consultancy-specific block", "Tier 2"

**Authored block**:
A block a consultant generates on demand for a specific document need, and may share peer-to-peer with colleagues by email. Auto-installs on receipt if it passes the lint + watchdog gate; otherwise quarantined.
_Avoid_: "user-created block", "custom block", "ad-hoc block", "Tier 3"

### Brand & rendering

**Brand tokens**:
The single machine-readable source for colours, fonts, spacing, and logo. Derived from demo materials at setup; consumed by every renderer. There is exactly one authoritative file per consultancy.
_Avoid_: "theme", "brand config", "style variables"

**Renderer**:
A React component (one per block type) that turns one block of the DocModel into HTML. Lives in `src/renderer/blocks/`. Must render without the editor.
_Avoid_: "view", "template"

**Node view**:
A React component (one per block type) that lets the editor show and edit one block in-place inside TipTap. Lives in `src/editor/nodes/`. A different concern from a Renderer — the node view is editable, the Renderer is print-quality output.
_Avoid_: conflating with "Renderer"

### Authored block transport

**Manifest header**:
The strict-format comment block at the top of every Authored block `.tsx` file. Carries scaffold version, generator model, original prompt, sender email, and timestamp. Source of identity, traceability, and the data needed to regenerate the block against a new scaffold.
_Avoid_: "header", "metadata block", "frontmatter"

**Scaffold version**:
The version of the per-tier block scaffold a generated block was produced against. Bumped when the manifest shape, the allowed imports, or the lint changes. Used at receive time to decide whether an Authored block needs regeneration.
_Avoid_: "block version", "schema version" (that's the DocModel's)

**Quarantine**:
The state an Authored block enters when it fails the lint, fails the watchdog, or arrives with a stale scaffold version. Visible to the recipient with a reason and (where applicable) a "Regenerate" action. Distinct from `/pending/` in D-09, which is the human-review staging area for Brand blocks.
_Avoid_: "pending" (overloaded), "rejected"

### Comments & patches

**Comment**:
A consultant-authored instruction to the AI, anchored to a `blockId` (and optionally a char range within that block). Stored on the DocModel, not in editor state, so it survives serialization.
_Avoid_: "annotation", "note"

**AI proposal**:
A structured patch returned by the LLM as one entry in a comment thread. Scoped to a single block. Never auto-applied — the consultant accepts or rejects it.
_Avoid_: "AI edit", "suggestion"

**Block patch**:
A structured operation that replaces or updates one block in the DocModel. The only shape the LLM is allowed to return for an edit.
_Avoid_: "diff", "update"

## Flagged ambiguities

**"Document" vs "deck"** — both are kinds of `DocModel` (`kind: "document"` or `kind: "deck"`). When the context could mean either, say "DocModel" or qualify ("flowing document", "slide deck").

**"Block" in code vs in product copy** — the schema uses `block` consistently. Product copy may say "section" or "element"; in code, always "block".

## Deferred concepts

These have been discussed but are intentionally not built in v1. Listed here so contributors don't re-invent them when the topic resurfaces.

- **Organisation-shared Authored block library** — a place where Authored blocks from across a consultancy collect for colleagues to discover and pull (vs the current email-only sharing). Future improvement. See ADR-0004.
- **Extended Authored-block capabilities** — promotion of atom-node-with-JSON-payload, custom side panels, or ECharts/Mermaid embeds into the Authored tier (currently restricted to Standard/Brand). Triggered when (a) real usage shows the simple-container subset is too restrictive, and (b) the codegen + lint can vet the additional surface area. See ADR-0007.

## Example dialogue

> **Dev:** A consultant emailed me asking why their callout doesn't pick up the new brand colour.
>
> **Lead:** Is it a Standard callout or a Brand block based on it?
>
> **Dev:** Standard.
>
> **Lead:** Then it should be reading from Brand tokens automatically. Did the install ship the new tokens file?
>
> **Dev:** Yes, but the Renderer is showing the old colour.
>
> **Lead:** Check the Renderer in `src/renderer/blocks/Callout.tsx` — it might be hard-coding instead of resolving the token. The Node view in `src/editor/nodes/CalloutNode.tsx` is separate; don't get them confused.
